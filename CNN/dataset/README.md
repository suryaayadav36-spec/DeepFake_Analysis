# CNN Dataset

Place labeled image files in this structure:

```text
CNN/dataset/
  train/
    real/
    fake/
  val/
    real/
    fake/
```

Use balanced counts where possible. A practical hackathon baseline is at least 500 real and 500 fake training images, plus 100 and 100 validation images.
